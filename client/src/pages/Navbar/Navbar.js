import React from "react";
import styled from "styled-components";

const Navbar = () => {
  return <Nav>COVID Country Tracker</Nav>;
};

export default Navbar;

const Nav = styled.nav`
  text-align: center;
  color: white;
  font-size: 2rem;
  padding: 2rem 0;
`;
